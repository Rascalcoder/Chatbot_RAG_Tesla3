HUGGINGFACE_HUB_TOKEN
# REPLACE_ME with your HuggingFace token (do NOT commit this file)
# Example usage:
# 1) Use `huggingface-cli login` (recommended) OR
# 2) Create a local `.env` with HUGGINGFACE_HUB_TOKEN=hf_xxx... (do NOT commit)
